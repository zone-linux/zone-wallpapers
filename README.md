# zone-wallpapers

S.T.A.L.K.E.R.-themed wallpapers for Zone Linux.

Installed to `/usr/share/backgrounds/zone/` by the `zone-wallpapers` package.

Referenced by:
- `zone-xfce` XFCE desktop config (`xfce4-desktop.xml`)
- LightDM GTK greeter config (background image)
- Conky config (`zone.conkyrc`)
